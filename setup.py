#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='coop_marl',
      version='0.0.1',
      description='',
      packages=find_packages(),
      install_requires=[]
    )