#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='rebar',
      version='0.0.1',
      description='rebar is Andy Jones’s personal reinforcement learning toolbox.',
      packages=find_packages(),
      python_requires='>=3.6',
      install_requires=[]
    )
