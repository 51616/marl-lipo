# Format goes like this
# def_config:
#   k: v
# config:
#   k: v
def_config: !include config/algs/trajedi/default.yaml

runner: StepsRunner
n_iter: 2000
n_sp_ts: 10000
eval_interval: 500
pop_size: 4
diverse_coef: 10.0
kernel_gamma: 0.0
lr: 0.0005
num_mb: 5
epochs: 15
ent_coef: 0.03
